# Diagnostics

Generated by `scripts/build-diagnostics-doc.mjs` from `RULES` (`src/diagnostics/rules.ts`, task 14) - do not hand-edit. Every rule cites the RRF source or wiki passage that justifies it; see `docs/tasks/14-diagnostics.md` for what's deliberately NOT implemented and why.

## Syntax

| id | severity | description | sources |
| --- | --- | --- | --- |
| `syntax/lexer-error` | error | A line failed to lex at all - an unterminated quoted string, an unterminated CNC bracketed comment, or an unbalanced { in an expression. | dwc-gcode-core src/lex.ts scanContent - each condition cited to RRF's own StringParser.cpp at its own definition site (task 05) |
| `syntax/line-too-long` | error | The line's own non-comment content is 256 characters or more (RRF's line buffer, including a null terminator, so 255 usable) - RRF throws "GCode command too long". A CNC (...) bracketed comment counts toward this; a trailing ; comment does not. | RRF 3.7.0-rc.1 Config/Configuration.h:169 MaxGCodeStringLength = 256<br>RRF 3.7.0-rc.1 GCodes/GCodeBuffer/StringParser.cpp:74-97,385-387 (buffer allocation, overflow flag, thrown exception) |
| `syntax/checksum-mismatch` | error | A line has an N<num> line number and a *NN checksum (1-3 digits, the classic XOR form), but the checksum doesn't match the line's own content. | RRF 3.7.0-rc.1 GCodes/GCodeBuffer/StringParser.cpp:61-73 AddToChecksum/StoreAndAddToChecksum (XOR of every byte before *)<br>RRF 3.7.0-rc.1 GCodes/GCodeBuffer/StringParser.cpp:328-341 badChecksum check<br>RRF 3.7.0-rc.1 GCodes/GCodes2.cpp:4719 "Checksum error on line %d" |

## Structure

| id | severity | description | sources |
| --- | --- | --- | --- |
| `structure/document-error` | error | A structural error in the meta-gcode block tree - elif/else without a matching if, else after an else, break/continue outside a loop, a T command not alone on its line, or mixed space/tab indentation (RRF warns once per file for this last one). | dwc-gcode-core src/document.ts block-builder (task 06), each cited at its own definition site to StringParser::ProcessIfCommand/ProcessElseCommand/ProcessElifCommand/ProcessWhileCommand/ProcessBreakCommand/ProcessContinueCommand |
| `structure/macro-command-not-last` | warning | G28, G29, G32 or M98 shares a line with another command - the wiki says a macro-invoking command must be alone on its line. | wiki Gcodes.md "Multiple commands on a single line" |
| `structure/capitalised-meta-keyword` | warning | A line's own content case-insensitively matches a meta keyword (if/elif/else/while/break/continue/abort/var/global/set/echo/skip) but isn't all-lowercase - RRF's own recognition is case-sensitive, so this line is not read as a meta-command at all and is silently ignored. | dwc-gcode-core src/metaKeywords.ts metaKeywordOf - cited to RRF's own ProcessConditionalGCode length-then-text dispatch (task 06) |

## Dictionary

| id | severity | description | sources |
| --- | --- | --- | --- |
| `dictionary/unknown-command` | info | The command isn't in this package's dictionary at all. RRF itself would try to run /sys/<code>.g for it (its own "custom G/M codes" mechanism) - reported at info without a project (no way to know if that file exists), and only when checked against a project that's missing the file does this become more severe (see project/missing-macro-file). | RRF 3.7.0-rc.1 GCodes/GCodes2.cpp:4810 GCodes::TryMacroFile<br>wiki Gcodes.md "Custom G and M codes" |
| `dictionary/unknown-parameter` | warning | A parameter letter this command's reviewed dictionary entry doesn't recognise (and the command has no generic axisParameters catch-all). Only checked against REVIEWED entries - a draft-only entry's parameter list is a heuristic, not a fact, so this rule stays silent for those. | dwc-gcode-core dictionary/commands.json (task 10) - each reviewed entry's own parameter list, itself cited to RRF source |
| `dictionary/wrong-kind` | warning | A parameter's value doesn't look like the kind the dictionary says it should be (e.g. a non-numeric value where a number is expected) - unless the value is an RRF {...} expression, which is always allowed where expressionAllowed is true and can't be shape-checked statically. | dwc-gcode-core dictionary/commands.json (task 10) ParamSpec.kind |
| `dictionary/missing-required` | error | A parameter the dictionary marks required: true is absent from the line. | dwc-gcode-core dictionary/commands.json (task 10) ParamSpec.required - only set true where RRF's own gb.MustSee(...) is read directly, per task 10's own rule |
| `dictionary/value-out-of-range` | warning | A literal numeric parameter's value falls outside the dictionary's own range, or (for an enumerated parameter) isn't one of its listed values. | dwc-gcode-core dictionary/commands.json (task 10) ParamSpec.range/.values |
| `dictionary/not-available-on-firmware` | error | A command or parameter the dictionary dates with since/until isn't present at the target firmware version. | dwc-gcode-core dictionary/commands.json (task 10/12) CommandSpec.since/until, ParamSpec.since/until |
| `dictionary/deprecated` | warning | A command the dictionary marks deprecated, with its replacement if one is known. | dwc-gcode-core dictionary/commands.json (task 10) CommandSpec.deprecated |
| `dictionary/wrong-machine-mode` | warning | A command the dictionary restricts to specific machine modes (machineModes) is used in a document set to a different one. | dwc-gcode-core dictionary/commands.json (task 10) CommandSpec.machineModes |

## Project

| id | severity | description | sources |
| --- | --- | --- | --- |
| `project/undefined-symbol` | error | A tool/heater/sensor/fan/axis/probe/endstop/accelerometer/spindle/global/filament is referenced but never defined anywhere in the project. NOT checked for extruder/driver: SYMBOL_RULES only records USES for those two (RRF has no single command that "creates" an extruder or driver number the way M950/M563/M308 do for the others), so "undefined" would misfire on every ordinary config. | dwc-gcode-core src/project.ts SYMBOL_RULES (task 13) |
| `project/duplicate-definition` | warning | The same numbered/named resource is defined more than once (unconditionally) in the project - except a filament, whose config.g/load.g/unload.g each legitimately add their own definition site for the same name by design. | dwc-gcode-core src/project.ts (task 13) - a definition site's own conditional flag; addFilamentSymbols's own per-file-kind definition sites |
| `project/order-dependency` | warning | A command the dictionary says must follow another (mustFollow) is used before that other command is ever defined/run anywhere earlier in the project. | dwc-gcode-core dictionary/commands.json (task 10) CommandSpec.mustFollow |
| `project/missing-macro-file` | error | A call this package's invocation table recognises (M98, G28/homing, a tool change, M701/M702, G29/G32, pause/resume, M501, M581, ...) doesn't resolve to a file present in the project. | dwc-gcode-core docs/invocation-table.md, src/project.ts Project.calls.resolved (task 13) |

## Release

| id | severity | description | sources |
| --- | --- | --- | --- |
| `release/impact` | warning | A command, parameter, object-model path or expression-syntax feature this file uses changed between the file's own stamped RRF version and the target version (task 12's impactOf) - in either direction. | dwc-gcode-core src/releases/changes.ts, src/releases/impact.ts (task 12) |

## Menu

| id | severity | description | sources |
| --- | --- | --- | --- |
| `menu/unknown-command` | error | A menu file's command word isn't one of the six RRF recognises (image, text, button, value, alter, files). | RRF 3.7.0-rc.1 Display/Menu.cpp Menu::ParseMenuLine (task 08) |
| `menu/target-missing` | error | A "menu" action's target isn't a menu file present in the project - either the L parameter (the common "A\"menu\" L\"name\"" form: RRF's own case 'L' sets "fname", which is what "menu" chains to) or a name embedded right in the action text ("menu <name>", the separate form EncoderAction_ExecuteHelper also recognises). | RRF 3.7.0-rc.1 src/Display/Menu.cpp:39 "'menu' (chains to the menu file given in the L parameter)"<br>RRF 3.7.0-rc.1 src/Display/Menu.cpp:357-366 case 'L' sets fname<br>RRF 3.7.0-rc.1 src/Display/Menu.cpp:580 EncoderAction_ExecuteHelper StringStartsWithIgnoreCase(cmd, "menu ") |
| `menu/image-missing` | error | An image command's own file (its L parameter - RRF's "fname", the same letter "menu" chains through) isn't present in the project's 0:/menu/ directory. | RRF 3.7.0-rc.1 src/Display/Menu.cpp:357-366 case 'L' sets fname<br>RRF 3.7.0-rc.1 src/Display/Menu.cpp:407 new ImageMenuItem(row, column, fname) |

## Data

| id | severity | description | sources |
| --- | --- | --- | --- |
| `data/height-map-error` | error | heightmap.csv failed to load - reported with exactly the message RRF's own loader would give. | RRF 3.7.0-rc.1 Movement/BedProbing/Grid.cpp (task 08's parseHeightMap) |

## Object model

| id | severity | description | sources |
| --- | --- | --- | --- |
| `objectModel/unknown-path` | error | An object-model path referenced in an expression doesn't exist at the target firmware version. | dwc-gcode-core src/objectmodel/schema.ts objectModelPath (task 11) |
| `objectModel/deprecated-path` | warning | An object-model path referenced in an expression is deprecated at the target firmware version, with the deprecation message. | dwc-gcode-core src/objectmodel/schema.ts objectModelPath (task 11) |

